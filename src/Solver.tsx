import { type FC } from "react";

import { FlowViewer } from "./components";

export const Solver: FC = () => {
  return (
    <div id="solver">
      <FlowViewer />
    </div>
  );
};

export default Solver;
